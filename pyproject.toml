[project]
name = "sententia-web"
version = "0.1.0"
description = "Monorepo de Sententia - Asistente Legal"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.2.15",
]

[tool.uv.workspace]
members = ["backend"]

[tool.uv]
package = false
